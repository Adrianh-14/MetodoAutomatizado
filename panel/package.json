{
  "name": "metodo-automatizado",
  "private": true,
  "scripts": {
    "build": "cd backend && pnpm run build && cd ../frontend && pnpm run build",
    "install:all": "cd backend && pnpm install && cd ../frontend && pnpm install"
  }
}
